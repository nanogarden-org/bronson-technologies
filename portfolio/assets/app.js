const b=document.querySelector('#q'),o=document.querySelector('#results');if(b&&o){fetch('data/search-index.json').then(r=>r.json()).then(d=>{let a=[];Object.entries(d).forEach(([k,v])=>v.forEach(x=>a.push({kind:k,...x})));b.oninput=()=>{let q=b.value.toLowerCase().trim();let h=a.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,30);o.innerHTML=q?h.map(x=>`<div class="card"><strong>${x.name||x.title}</strong><br><small>${x.kind}</small><p>${x.summary||x.issuer||''}</p>${x.url||x.validator?`<a href="${x.url||x.validator}">Open evidence</a>`:''}</div>`).join(''):''}})}