# Credentials

This directory indexes relevant credentials by provider. Prefer official verification URLs, credential name, issuer, issue date, current status, and the project where the learning was applied.

Do not commit account credentials, private learner IDs, QR codes containing sensitive data, or unredacted certificate files without review.
